{
    "name": "typhoncath/crm",
    "description": "Typhon Cath CRM — customer, RFQ, campaign and inventory management.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.2",
        "ext-pdo": "*",
        "ext-mbstring": "*",
        "ext-dom": "*",
        "dompdf/dompdf": "^3.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "lint": "find app public config tests database -name '*.php' -print0 | xargs -0 -n1 php -l",
        "test:static": [
            "@php tests/csrf_coverage.php",
            "@php tests/authz_coverage.php"
        ],
        "test:unit": "phpunit --testsuite unit",
        "test:integration": "phpunit --testsuite integration",
        "test": [
            "@test:static",
            "phpunit"
        ]
    },
    "scripts-descriptions": {
        "lint": "php -l over every non-vendor PHP file",
        "test:static": "CSRF and authorization wiring harnesses (no server, no DB)",
        "test:unit": "PHPUnit unit suite (no database)",
        "test:integration": "PHPUnit integration suite (requires MySQL — see docs/setup.md)",
        "test": "Static harnesses plus the full PHPUnit suite"
    },
    "config": {
        "optimize-autoloader": true,
        "allow-plugins": false,
        "sort-packages": true
    }
}
